---
name: text-card
description: 当需要制作文字卡片图片时使用，包括生成卡片 HTML，并调用底层 HTML 转图片能力输出最终图片。适用于小红书、公众号图文等场景，也适用于其他文字卡片图片生成任务。
---

# 文字卡片

当任务是制作文字卡片图片时，使用这个 skill。适合：

- 小红书卡片图或封面图
- 公众号图文配图或封面图
- 其他需要先生成 HTML 再导出图片的文字卡片场景

默认优先基于现有模板或示例复制修改，不要先从零生成。

## 工作流

1. 收集卡片文案和基础样式信息。
2. 优先查看 `assets/` 模板和 `references/` 示例。
3. 如果用户没有明确要求其他风格或结构，直接复用 `references/` 下最贴近需求的示例 HTML，替换文案并做少量必要微调；只有示例明显不适合时才从零生成。
4. 确保最终 HTML 中有明确的卡片容器，推荐使用 `id="card"`。
5. 调用底层 `html-to-image-render` skill 渲染图片，并检查输出结果。
6. 如果用户没有指定风格，且首版是基于示例模板生成的，交付时主动问一句是否需要继续调整颜色、排版、字号、留白或装饰细节。

## 重要要求

- 当 `references/` 下已有可复用的完整示例，且用户没有明确要求“换一种风格”“重新设计”“不要参考模板”等指令时，必须直接基于最接近需求的参考示例修改，不要自行重做版式。
- 基于参考示例修改，默认只做这些改动：替换文案、微调字号、行距、颜色，增减极少量装饰或 emoji，以保证内容能稳定放入原版式。
- 只有在以下情况才允许不直接复用参考示例：参考示例无法容纳目标文案、结构与任务类型明显不匹配、或用户明确要求新的视觉风格。
- 如果决定不复用参考示例，必须先明确说明原因。

## 当前资源

可参考模板：

- `assets/quote-text-card-template.html`

可参考示例：

- `references/example-quote-text-card-purple.html`
- `references/example-quote-text-card-blue.html`

说明：

- `quote-text-card-template.html` 适合引用式、大字号、单段正文高亮类卡片，也适合延展为标题型封面图。
- `example-quote-text-card-purple.html` 和 `example-quote-text-card-blue.html` 都可直接参考排版、结构和配色。

## 编写约定

- 默认输出单张卡片布局，核心内容放在 `#card` 容器中。
- 使用内联 `style` 或 `<style>`，避免依赖外部样式文件。
- 明确设置背景、字体大小、行高和内边距。
- 如果用户没有明确要求，主文案尽量控制在 40 个字以内，优先短句，避免信息过载。
- 默认优先保持纯色背景和清晰排版，不要主动加入复杂渐变、光晕或大面积装饰；只有在用户明确要求更强视觉风格时再加。
- 如果需要远程图片、Logo、字体，预留足够的加载等待时间给底层渲染。

## 注意事项

- 渲染方式、命令参数和视口设置，统一参考 `html-to-image-render` skill。
- 输出图片文件扩展名应与 `--image-type` 保持一致。
- 如果底层渲染报 `element_id` 不存在，先检查 HTML 中是否真的存在 `id="card"`。
